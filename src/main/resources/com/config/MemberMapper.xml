<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.config.MemberMapper">

    <select id="selectAllMember" resultType="MemberDTO">
        select *
        from MEMBER
    </select>

    <!-- 회원 추가-->
    <insert id="memberAdd" parameterType="MemberDTO">
        INSERT	INTO MEMBER (userid, passwd, name, phone1, phone2, phone3, email1, email2, image, image_db)
        VALUES (#{userid},#{passwd},#{name},#{phone1},#{phone2},#{phone3},#{email1},#{email2}, #{image}, #{image_db})
    </insert>
</mapper>